// import { selectCategoriesPageDomain } from '../selectors';

describe('selectCategoriesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
